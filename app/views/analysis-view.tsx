"use client";

import { useEffect, useRef, useState, type ChangeEvent } from "react";
import type { Course, Insight, Material } from "@/lib/types";
import type { AnalysisAttempt, CourseAiRecord } from "../ui-types";
import { formatExamDate, formatFileSize, initials, materialKindForFile, validateUploadFile } from "../ui-helpers";

export default function AnalysisView({ selectedCourse, courseList, materials, insights, aiRecord, insightsAreSynthesized, onSelectCourse, onUploadAndAnalyze, onRetryAnalysis, onSynthesize, onAddCourse, onEditCourse, onDeleteCourse, onDownloadMaterial, onDeleteMaterial }: { selectedCourse: Course; courseList: Course[]; materials: Material[]; insights: Insight[]; aiRecord?: CourseAiRecord; insightsAreSynthesized: boolean; onSelectCourse: (id: string) => void; onUploadAndAnalyze: (file: File) => Promise<AnalysisAttempt>; onRetryAnalysis: (id: string) => Promise<boolean>; onSynthesize: () => void; onAddCourse: () => void; onEditCourse: (course: Course) => void; onDeleteCourse: (course: Course) => void; onDownloadMaterial: (material: Material) => void; onDeleteMaterial: (material: Material) => void }) {
  const inputRef = useRef<HTMLInputElement>(null);
  const [selectedFile, setSelectedFile] = useState<File | null>(null);
  const [pendingMaterialId, setPendingMaterialId] = useState<string | null>(null);
  const [selectionError, setSelectionError] = useState("");
  const isWorking = aiRecord?.status === "analyzing" || aiRecord?.status === "synthesizing";
  const analyzedCount = materials.filter((material) => material.status === "已分析").length;

  useEffect(() => { setSelectedFile(null); setPendingMaterialId(null); setSelectionError(""); if (inputRef.current) inputRef.current.value = ""; }, [selectedCourse.id]);

  const onSelectFile = (event: ChangeEvent<HTMLInputElement>) => {
    const file = event.target.files?.[0];
    event.target.value = "";
    if (!file) return;
    const error = validateUploadFile(file);
    if (error) { setSelectedFile(null); setSelectionError(error); return; }
    setSelectionError("");
    setPendingMaterialId(null);
    setSelectedFile(file);
  };

  const onStartAnalysis = async () => {
    if (isWorking) return;
    if (pendingMaterialId) {
      const analyzed = await onRetryAnalysis(pendingMaterialId);
      if (analyzed) {
        setPendingMaterialId(null);
        setSelectedFile(null);
      }
      return;
    }
    if (!selectedFile) return;
    const result = await onUploadAndAnalyze(selectedFile);
    if (result.analyzed) {
      setSelectedFile(null);
    } else if (result.persisted && result.materialId) {
      setPendingMaterialId(result.materialId);
    }
  };

  return <div className="view-content"><div className="page-heading"><div><p className="eyebrow">MATERIAL INTELLIGENCE</p><h2>从资料里找到真正重要的内容</h2><p>文件先保存在本地工作区，再由 AI 提取可追溯考点与练习。</p></div><div className="analysis-heading-actions"><button className="outline" disabled={isWorking} onClick={onSynthesize}>✦ {aiRecord?.status === "synthesizing" ? "正在综合…" : analyzedCount ? `综合 ${analyzedCount} 份资料` : "综合本课程资料"}</button><button className="outline" onClick={onAddCourse}>＋ 新增课程</button></div></div><div className="analysis-layout"><aside className="course-selector"><p>我的课程</p>{courseList.map((course) => <button className={course.id === selectedCourse.id ? "selected" : ""} onClick={() => onSelectCourse(course.id)} key={course.id}><span style={{ backgroundColor: course.color }}>{initials(course.name)}</span><div><b>{course.name}</b><small>{course.code} · {course.teacher}</small></div><i>{course.mastery}%</i></button>)}</aside><div className="analysis-main"><section className="course-header"><div><span className="course-badge" style={{ backgroundColor: selectedCourse.color }}>{initials(selectedCourse.name)}</span><div><h3>{selectedCourse.name}</h3><p>{selectedCourse.code} · {selectedCourse.teacher} · 考试 {formatExamDate(selectedCourse.examDate)}</p></div></div><div className="course-header-actions"><strong>{selectedCourse.mastery}%<small>当前掌握度</small></strong><button className="text-button" type="button" onClick={() => onEditCourse(selectedCourse)}>编辑</button><button className="text-button danger-button" type="button" onClick={() => onDeleteCourse(selectedCourse)}>删除课程</button></div></section><section className="upload-zone" aria-busy={isWorking} onDragOver={(event) => { event.preventDefault(); event.dataTransfer.dropEffect = "copy"; }} onDrop={(event) => { event.preventDefault(); const file = event.dataTransfer.files?.[0]; if (!file || isWorking) return; const error = validateUploadFile(file); if (error) { setSelectedFile(null); setSelectionError(error); return; } setSelectionError(""); setPendingMaterialId(null); setSelectedFile(file); }}><input ref={inputRef} data-testid="material-file-input" className="file-picker-input" id={`material-upload-${selectedCourse.id}`} type="file" accept=".pdf,.ppt,.pptx,.doc,.docx,.jpg,.jpeg,.png,.webp" onChange={onSelectFile} disabled={isWorking} aria-label="从本机选择资料文件" /><div className="upload-copy"><span aria-hidden="true">{isWorking ? "◌" : "↑"}</span><div><b>{isWorking ? "AI 正在处理资料" : "添加一份私有资料"}</b><p>PDF、PPT、Word 或图片 · 单份不超过 50 MB</p></div></div><div className="upload-actions"><button data-testid="choose-local-file" className="outline" type="button" onClick={() => inputRef.current?.click()} disabled={isWorking}>{selectedFile ? "重新选择文件" : "从本机选择文件"}</button><button data-testid="start-ai-analysis" className="primary" type="button" onClick={() => void onStartAnalysis()} disabled={(!selectedFile && !pendingMaterialId) || isWorking}>{isWorking ? "AI 分析中…" : pendingMaterialId ? "继续 AI 分析" : "保存并开始分析"}</button><small>{pendingMaterialId ? "文件已保存；配置 AI 后点击“继续 AI 分析”即可继续，文件不会重复上传。" : "点击后会先保存文件；如果 AI 配置缺失，资料仍会保留并可稍后重试。"}</small></div></section>{(selectedFile || selectionError) && <section className={`selected-file-card ${selectionError ? "error" : ""}`} aria-live="polite">{selectedFile ? <><div className="selected-file-icon">{materialKindForFile(selectedFile) === "课件" ? "PPT" : materialKindForFile(selectedFile) === "题库" ? "DOC" : materialKindForFile(selectedFile) === "讲义" ? "IMG" : "PDF"}</div><div><b>{selectedFile.name}</b><p>{formatFileSize(selectedFile.size)} · 将归入「{selectedCourse.name}」</p><small>{pendingMaterialId ? "文件已保存。配置 AI 后点击“继续 AI 分析”即可继续，文件不会重复上传。" : "选择文件后尚未上传；点击“保存并开始分析”才会创建持久化资料。"}</small></div><button type="button" className="clear-selected-file" onClick={() => { setSelectedFile(null); setPendingMaterialId(null); }} aria-label={`移除 ${selectedFile.name}`}>×</button></> : <><span>!</span><div><b>无法选择该资料</b><p>{selectionError}</p></div></>}</section>}{aiRecord && <section className={`analysis-agent-card ${aiRecord.status}`}><div className="analysis-agent-top"><div><span>✦</span><p>AI 分析 {aiRecord.status === "analyzing" ? "进行中" : aiRecord.status === "synthesizing" ? "正在综合" : aiRecord.status === "error" ? "需留意" : "已就绪"}</p><h3>{aiRecord.synthesized ? "已生成课程级复习依据" : "已保留资料分析结果"}</h3></div><small>{aiRecord.provider} · {aiRecord.model}<b>{aiRecord.documentCount} 份资料</b></small></div><p className="analysis-agent-summary">{aiRecord.summary}</p>{aiRecord.studyActions.length > 0 && <div className="analysis-actions">{aiRecord.studyActions.slice(0, 3).map((action) => <span key={action}>{action}</span>)}</div>}{aiRecord.warnings.length > 0 && <p className="analysis-warning">提示：{aiRecord.warnings.join("；")}</p>}</section>}{aiRecord && aiRecord.questionPatterns.length > 0 && <section className="analysis-section"><div className="section-heading"><div><p className="eyebrow">QUESTION PATTERNS</p><h2>识别到的题型规律</h2></div><span>{aiRecord.questionPatterns.length} 条</span></div><div className="pattern-list">{aiRecord.questionPatterns.map((pattern) => <article className="pattern-card" key={`${pattern.title}-${pattern.evidence.location}`}><b>{pattern.title}</b><span>{pattern.type}</span><p>{pattern.description}</p><small>⌁ {pattern.evidence.label} · {pattern.evidence.location}</small></article>)}</div></section>}<section className="analysis-section"><div className="section-heading"><div><p className="eyebrow">EVIDENCE-BASED INSIGHTS</p><h2>高频与薄弱考点</h2></div><span>{insights.length} 个已识别 · {insightsAreSynthesized ? "跨资料综合" : "单份资料信号"}</span></div><div className="insight-list">{insights.length ? insights.map((item) => <article key={item.id}><div className="insight-title"><span className={`trend ${item.trend}`}>{item.trend}</span><h3>{item.title}</h3><strong>重要度 {item.importance}/5 · 出现 {item.frequency} 份</strong></div><p>{item.summary}</p><div className="source-links">{item.sources.map((source) => <span className="source-link" key={source}>⌁ {source}</span>)}</div><div className="mastery-inline"><span>掌握度</span><div className="bar"><i style={{ width: `${item.mastery}%` }} /></div><b>{item.mastery}%</b></div></article>) : <p className="empty-state">尚未识别考点。上传并完成第一份资料分析后，结果会保存在这里。</p>}</div></section><section className="analysis-section"><div className="section-heading"><div><p className="eyebrow">PRIVATE MATERIALS</p><h2>课程资料</h2></div></div><div className="material-grid">{materials.length ? materials.map((material) => <article className="material-card" key={material.id}><div className={`file-icon ${material.kind}`}>{material.kind === "试卷" ? "PDF" : material.kind === "课件" ? "PPT" : material.kind === "题库" ? "DOC" : "IMG"}</div><div><div className="material-title"><b>{material.name}</b></div><p>{material.pages ? `${material.pages} 页 · ` : ""}{material.createdAt}</p><small className={`status ${material.status}`}>{material.status}</small><em>{material.source}</em>{material.error && <em className="material-error">{material.error}</em>}</div><div className="material-actions"><button className="material-action" type="button" onClick={() => onDownloadMaterial(material)}>下载</button>{material.status !== "已分析" && <button className="material-action" type="button" disabled={isWorking} onClick={() => onRetryAnalysis(material.id)}>{material.status === "分析中" ? "分析中…" : "重试分析"}</button>}<button className="material-action danger-button" type="button" onClick={() => onDeleteMaterial(material)}>删除</button></div></article>) : <p className="empty-state">尚无资料。选择本机文件后会安全保存在本地工作区。</p>}</div></section></div></div></div>;
}

