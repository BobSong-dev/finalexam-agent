"use client";

import type { AiStatus } from "@/lib/ai-types";
import ModalShell from "./modal-shell";

export default function AiSettingsModal({ aiStatus, aiStatusLoading, customBaseUrlDisabled, sessionApiKey, sessionBaseUrl, apiKeyDraft, baseUrlDraft, model, modelOptions, onClose, onRefresh, onKeyChange, onBaseUrlChange, onModelChange, onSave, onClear }: { aiStatus: AiStatus | null; aiStatusLoading: boolean; customBaseUrlDisabled: boolean; sessionApiKey: string; sessionBaseUrl: string; apiKeyDraft: string; baseUrlDraft: string; model: string; modelOptions: string[]; onClose: () => void; onRefresh: () => void; onKeyChange: (value: string) => void; onBaseUrlChange: (value: string) => void; onModelChange: (value: string) => void; onSave: () => void; onClear: () => void }) {
  return <ModalShell className="ai-settings-modal" labelledBy="ai-settings-title" onClose={onClose}><p className="eyebrow">AI 分析设置</p><h2 id="ai-settings-title">配置本次会话的 AI 分析</h2><p className="modal-intro">Key、兼容地址和模型名只保存在当前浏览器会话中，并仅通过服务端转发给兼容 API。</p><section className={`ai-availability ${aiStatus?.configured ? "available" : "unavailable"}`}><div><span>{aiStatusLoading ? "◌" : aiStatus?.configured ? "✓" : "!"}</span><p><b>{aiStatusLoading ? "正在检查服务端配置" : aiStatus?.configured ? "已检测到服务端环境 Key" : "尚未检测到服务端环境 Key"}</b><small>{aiStatus?.configured ? `也可使用会话 Key；默认模型 ${aiStatus.defaultModel}` : "输入自己的会话 Key 后即可开始真实分析。"}</small></p></div><button className="text-button" onClick={onRefresh}>刷新</button></section>{aiStatus && !aiStatus.customBaseUrlAllowed && <p className={`base-url-policy-warning ${customBaseUrlDisabled ? "saved" : ""}`}>当前部署未启用用户自定义请求地址；保留为空时会使用服务端默认地址。</p>}<label className="field-label">OpenAI API Key（仅本次会话）<input type="password" autoComplete="off" spellCheck={false} value={apiKeyDraft} onChange={(event) => onKeyChange(event.target.value)} placeholder="sk-..." /></label><p className="session-note">关闭浏览器会话后自动清除，不会写入 localStorage。</p><label className="field-label">OpenAI 兼容 API 根地址（可选）<input type="url" inputMode="url" autoComplete="url" spellCheck={false} value={baseUrlDraft} onChange={(event) => onBaseUrlChange(event.target.value)} placeholder="https://api.openai.com/v1" /></label><p className="session-note">需兼容 <b>/files</b> 和 <b>/responses</b>；自定义地址必须同时填写本次会话 Key，服务端环境 Key 只用于服务端默认地址。</p><label className="field-label">模型名称（可自定义）<input list="ai-model-options" value={model} onChange={(event) => onModelChange(event.target.value)} placeholder="gpt-5-mini" /><datalist id="ai-model-options">{modelOptions.map((item) => <option key={item} value={item} />)}</datalist></label><div className="ai-settings-actions"><button className="primary" onClick={onSave}>{sessionApiKey || sessionBaseUrl ? "更新会话配置" : "保存会话配置"}</button>{(sessionApiKey || sessionBaseUrl) && <button className="outline" onClick={onClear}>清除会话配置</button>}</div><p className="ai-footnote">模型名会由服务端校验；可使用兼容服务商的合法模型标识。</p></ModalShell>;
}
